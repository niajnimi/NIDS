# NIDS
A system that detects attack happening on the host device and generates a live attack graph with the alert and description of the packets. Also host have an option to save the logs of the scan, only alert logs, only packet logs.
